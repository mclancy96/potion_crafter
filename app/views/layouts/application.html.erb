<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Potion Crafter" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta charset="utf-8">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" type="image/png" href="/favicon_48x48.png" sizes="48x48">
    <link rel="apple-touch-icon" href="/favicon_512.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if logged_in? %>
      <%= render 'layouts/navbar' %>
    <% end %>
    <% flash.each do |key, message| %>
      <% alert_type = key == 'alert' ? 'danger' : 'info' %>
      <div class="flash alert alert-<%= alert_type %>" role="alert"><%= message %></div>
    <% end %>
    <div class="container text-white mt-3">
      <%= yield %>
    </div>
    <% unless @no_spacer %>
      <div class="spacer-bottom"></div>
    <% end %>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
    <script>
      document.addEventListener("turbo:render", function() {
        setTimeout(function() {
          document.querySelectorAll('.flash').forEach(function(flash) {
            flash.style.display = 'none';
          });
        }, 4000);
      });
    </script>
  </body>
</html>
