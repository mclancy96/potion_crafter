  <%= form_with model: [@potion, @review], local: true, data: { turbo: false }, class: "row g-3" do |form| %>
  <% if @review.errors.any? %>
    <div class="alert alert-danger mb-3">
      <h4 class="mb-2"><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h4>
      <ul class="mb-0">
        <% @review.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-lg-12 col-xl-6">
    <%= form.label :rating, class: "form-label" %>
    <%= form.number_field :rating, min: 1, max: 5, class: "form-control" %>
  </div>
  <div class="col-lg-12 col-xl-6">
    <%= form.label :comment, class: "form-label" %>
    <%= form.text_area :comment, class: "form-control", rows: 3 %>
  </div>
  <div class="col-12 text-center">
    <%= form.submit nil, class: "btn btn-primary" %>
  </div>
<% end %>
